{
  "name": "rental-contract-deployment",
  "version": "1.0.0",
  "description": "Deployment scripts for RentalContract",
  "scripts": {
    "compile": "truffle compile",
    "deploy": "truffle migrate --reset --network development"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "truffle": "^5.11.5"
  }
}
